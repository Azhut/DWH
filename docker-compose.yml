services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: dwh-app
    container_name: sport_api
    ports:
      - "2700:2700"
    environment:
      APP_ENV: "production"
      MONGO_URI: "mongodb://mongo:27017"
      DATABASE_NAME: "sport_data"
      API_HOST: "0.0.0.0"
      API_PORT: 2700
      DEBUG: "False"
      ENABLE_PROFILING: "false"
      FLATDATA_STORAGE: "duckdb"
      DUCKDB_PATH: "/data/duckdb/flat_data.duckdb"
    volumes:
      - duckdb_data:/data/duckdb
    depends_on:
      mongo:
        condition: service_healthy
    restart: unless-stopped

  mongo:
    image: mongo:5.0
    container_name: mongodb
    command: ["--setParameter", "diagnosticDataCollectionEnabled=false"]
    ports:
      - "2701:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mongo", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  mongodb_data:
  duckdb_data: