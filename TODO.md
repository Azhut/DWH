# TODO


## Infra

 

* [ ] В алапаевске должно быть 3878 записей, а сейчас пишется 3842
* [ ] Костыли в manual_map
---
* [ ] что-то не так с golden_snapshot, он явно не реагирует на изменения
* [ ] Поправить константы в generate_golden_snapshot. Странно, что есть Form_name, а не поулчается из бд

---

## Фильтры
* [ ] Проверить корректность фильтров и их скорость  

---


## База данных
* [ ] Переехатать на другую бд?
* [ ] Проверить единообразие обращения к бд 
* [ ] Работа с базой данных. Проверить ее асинхронность и транзакции. Например `FlatDataService.save_flat_data` использует `bulk_write`

---

## Развертывание 
* [ ] Пофиксить докер файлы (добавить\удалить helthcheck) 
* [ ] настроить сжатие в mongo, чтобы уменьшить размер данных
* [ ] в nginx убрать ограничение на размер файла

---
  


  

  



* * *

## Сделано 

* [x] Добавить проверку на дубликаты заголвоков
  
* [x] Добавить обрезку df если после заголвоков есть nan
  

* [x] Сущность "Город". Исправить название не фронте
  
* [x] Подумать над тем, чтобы собирать плоские данные по индексам столбцов и строк, а не по значениям. Могут быть дубли
  
  * Тут решение в том, что дублей вообще не должно быть. Если они нашлись, то CriticalParsingError
    
* [x] Откуда-то берутся дубли, не смог найти почему
  
  * Какая-то одноразовая ошибка, не получилось найти источник. Скорее всего просто была очищена коллеция FIles, но не очищена FlatData
    

* [x] Сохраняется мало `flat_data`, потому что null ячейки не сохраняются в flat_data, эта проблема есть для всех форм. -- снэпшот неправильынй для 1ФК. Проблема в FlatDataBuilder `app/domain/parsing/flat_data_builder.py`
* [x] При загрузке файла, который уже был загружен не проставляется id. При использовании API files могут выдаваться ошибочно загруженные. Как будто в коллекции files не нужно хранить информацию об ошибке. Но при upload нужно возвращать warning о том, какие файлы не были загружены, так как были ошибки с указанием ошибки. На фронте добавить нормальный формат отображения ошибки и даты, когда это произошло
* [x] В Files сохраняется файлы со статусом failed. Проверка уникальности не проходит. Может нужно добавить проверку уникальности только success файлам? А еще может если получается сохранить success файл, то удалять все ошибки которые были с таким же именем файла раньше?
* [x] индекс в монго странный, надо подумать еще
* [x] Проверить корректно ли отображается ошибка если файл загружен с ней. Типа если у меня в парсинге ошибка, то будет ли этот домен отправлен на фронт
* [x] странная ошибка {AttributeError}AttributeError("'UploadPipelineContext' object has no attribute 'extracted_data'")
  * Это сам дебаггер виноват, можно забить
* [x] В invalid_filter добавить статус 400 или 422 в ответ
* [x] Проверить корректность `regisrty.py` При перезагрузке кода (reload=True) или в многопроцессном режиме состояние может рассинхронизироваться.
* [x] странная тема с зависимостями
  
      get_parsing_strategy_registry(sheet_service=sheet_service)
      
      pipeline = build_default_pipeline(
            file_service=file_service,
            data_save_service=data_save_service, 
  
* [x] Вынести build_default_pipeline из upload_manager в file_processor где используется Pipeline
* [x] два раза читается файл, точно ли это нормально? два шага для этого создаются ReadFileContentStep и ProcessSheetsStep
* [x] Почистить архитектуру upload_pipeline 
* [x] Прибраться во всем проекте
* [x] Тест на снепшоты не работает корректно, судя по всему он не запускает пайплайн с нуля, а берет его откуда-то
* [x] Поправить генерацию снепшота
* [X] в снепшоте есть странная штука с разным количество заголвоков на каждом листе. Надо бы проверить
    * Это из-за скрытых колонок и столбца "Справочно"
* [x] Протестить 5ФК
* [x] Проверить размер файла почему так создается
  * Надо просто переименовать
* [x]  В "Справочно" нужно обрабатывать и пустые значения. Сейчас они не сохраняются
* [x] В notes_proccessor исправить логика на использование numpy
* [x] Добавить вывод результатов в тесты
* [x] Про то, что лежит в БД в FORM. Я создал вручную, но может стоило бы автоматически создавать точно существуюещие формы, если их нет.
* [x] странная тема с реквизитами форм. В 1ФК были отдельно реквзииты и отдельно список листов для пропуска. Протетстить всегда ли так новая форма создается
* [x] Добавить проверку на удаление форм 1ФК и 5ФК, их нельзя удалять. При удалении других форм удалять все что с ними связано
* [x] Протестить переход на автоматику в 1ФК, при поиске таблицы
* [x] Детект заголвоков тоже криво написан
* [x] Узнать количество файлов в год для 5ФК
* [x] Автоудаление логов
* [x] API отдачи логов на фронтенд
* [x] Нужно пофиксить logs: понять почему есть сущность Logs, а в сore есть дубликаты логики. Связь @app/domain/log/models.py , @app/core/exceptions.py и @app/core/logger.py .
* [x] Подумать, в каком виде сообщать ошибку на фронт. Я думаю error сохранять как есть, а в коллекцию логов писать больше инфы, в том числе домен
* [x] БД находит дубли по строкам и чистит их. Это происхоодит потому, что строки есть дублирующиеся типа "из них:"
  * Похоже что можно смотреть на -
  * в том числе может быть не всегда корректным, может быть интерсна только комбинация в том числе и пробелы или в том числе и -
* [x] Исправить поиск субъекта и года в названии файла
* [x] Давай ка если уровней больше чем vertical_hierarchy_max_path_segments, то кидать критическую ошибку, а не тихую обрезку
* [x] Парсинг в 1ФК вбирает в себя "Раздел I. Кадры | Код по ОКЕИ: человек - 792", нужно это поправить
* [x] фактический размер файла и то что пишется в files.flat_data отличается/
  * Есть утечка какая-то. Скорее всего из-за дублей, либо просто рассинхрон в значениях, когда расширяется flat data а size записывается раньше этого момента
---

* [ ] Почему вообще в коде есть вериткальные заголвоки и горизонтальные. Заменить бы их на строки и столбцы. ахахахах
